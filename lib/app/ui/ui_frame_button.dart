import 'package:flutter/material.dart';

class UiFrameButton extends StatelessWidget {
  const UiFrameButton({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}